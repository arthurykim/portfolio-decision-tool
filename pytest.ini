[pytest]
pythonpath = .
testpaths = tests
# Integration tests need live services (Milvus), so they stay out of the
# default run. `task test:integration` opts back in.
addopts = -m "not integration"
markers =
    integration: needs external services running (see tests/integration/)
